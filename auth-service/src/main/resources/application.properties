spring.application.name=auth-service

server.port=4003

# MySQL Configuration (same DB as UserInfo — auth-service only reads users for login)
spring.datasource.url=jdbc:mysql://localhost:3306/usersDB
spring.datasource.username=root
spring.datasource.password=756293
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Jwt - MUST match UserInfo's jwt.secret exactly, or tokens issued here won't validate there
jwt.secret=ThisIsADemoSecretKeyPleaseChangeIt32Bytes!
jwt.expiration-ms=3600000
